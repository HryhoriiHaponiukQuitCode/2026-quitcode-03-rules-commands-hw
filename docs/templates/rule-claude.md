---
paths:
  - "app/src/**/*.ts"
---

# <Назва правила>

<!-- Шаблон правила для Claude Code. Збережіть як .claude/rules/<назва>.md
     Поле frontmatter — саме `paths` (список шаблонів), не `globs`:
       з paths    — правило підтягується, коли агент читає файли, що підпадають під шаблон
       без paths  — правило завантажується на старті кожної сесії (як CLAUDE.md);
                    для такого правила frontmatter можна прибрати зовсім
     Цей коментар видаліть. -->

## Контекст

<Чому правило існує — одне-два речення, спираючись на materials/architecture-brief.md.>

## Правило

- <Конкретна перевірювана вимога: «вихідний HTTP — лише через postJson() з app/src/core/http.ts»>
- <Заборона разом з альтернативою: «не використовуй X — використовуй Y»>

## Як перевірити

- <Команда або ознака: `cd app && npm run check:rules` → 0 у рядку http-via-core>
