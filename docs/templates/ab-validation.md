# A/B-валідація правил (Task D)

> Скопіюйте в `docs/ab-validation.md` і заповніть. Порядок проведення — у
> `docs/walkthrough.md`, Task D. Числа беріть з виводу команд, а не з пам'яті.

**Інструмент і модель:** <напр. Claude Code 2.1.x · Sonnet 5 / Cursor 2.x · Auto>
**Запит:** `materials/ab-task.md` без змін, нова сесія для кожного прогону
**Що вимикали в B:** <які саме файли й теки перейменували>
**Базова лінія до прогонів:** `npm run check:rules` → TOTAL: <N>

## B — правила вимкнено

- `npm test`: <зелений / N тестів падає>
- `npm run check:rules`: TOTAL <N> → нові порушення: <правило — файл — рядок>
- Змінені й нові файли (`git status --short`): <...>
- Діф: `docs/ab/b-rules-off.diff`
- Що зробив агент — своїми словами: <...>

## A — правила ввімкнено

- `npm test`: <...>
- `npm run check:rules`: TOTAL <N> → нові порушення: <...>
- Змінені й нові файли: <...>
- Діф: `docs/ab/a-rules-on.diff`
- Що зробив агент: <...>

## Порівняння

| Що дивимось | B — без правил | A — з правилами |
|---|---|---|
| Нових порушень `check:rules` | | |
| HTTP | <fetch напряму / postJson> | |
| Токен і змінні середовища | <process.env / readEnv> | |
| Розбір відповіді Telegram | <JSON.parse / parseJson + guard> | |
| Журнал | <console / log> | |
| Зміни поза задачею (core, package.json) | | |
| Тести: скільки й що перевіряють | | |

## Висновок

<2–4 речення: чи змінили правила поведінку агента; яке правило спрацювало
найсильніше; що агент і без правил зробив правильно (це теж результат);
що ви змінили б у правилах після цього прогону.>
