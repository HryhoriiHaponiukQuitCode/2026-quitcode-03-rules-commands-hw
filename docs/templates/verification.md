# Перевірка (Task B, C і бонус E)

> Скопіюйте в `docs/verification.md` і заповніть. Сюди — лише те, що справді
> сталося у вашій сесії: цитати, числа, імена файлів.

## Task B — чи бачить інструмент AGENTS.md

- Інструмент і версія: <...>
- Як перевірив: <Claude Code — `/context`, розділ Memory files; Cursor — ...>
- Результат: <що саме побачили>

## Task C — прогони команд

### `/analyze-error`

- Виклик: `/analyze-error materials/error-log.txt`
- Чи підставився `$ARGUMENTS`: <так / ні — агент узяв ціль із тексту після команди>
- Що агент назвав корінною причиною (файл, рядок, механізм): <...>
- Чим відрізнив тригер від причини: <...>
- Чи зупинився там, де сказано в Stop: <...>

### `/refactor`

- Виклик: `/refactor <файл>`
- `npm run check:rules` для цього файлу: до <N> → після <N>
- `npm test` до / після: <...>
- Що змінилось у поведінці (має бути: нічого з того, що фіксують тести): <...>

### `/generate-integration`

- Виклик: `/generate-integration <сервіс>` (будь-який сервіс вашої агенції)
- Які файли створено: <...>
- `npm test`, `npm run check:rules`: <...>

## Task E (бонус) — хук

- Файли: <напр. `.claude/settings.json`, `.claude/hooks/protect-core.mjs`>
- Спроба змінити `app/src/core/...` → що відповів хук (цитата): <...>
