---
description: Коротко — коли це правило застосовується
globs: app/src/**/*.ts
alwaysApply: false
---

# <Назва правила>

<!-- Шаблон правила для Cursor. Збережіть як .cursor/rules/<назва>.mdc
     (розширення саме .mdc — файли .md у .cursor/rules Cursor ігнорує).
     Режим вмикається полями frontmatter:
       alwaysApply: true              — у кожній сесії (globs і description ігноруються)
       globs + alwaysApply: false     — коли в контексті є файл, що підпадає під шаблон
       лише description               — агент сам вирішує за описом, чи підтягнути правило
       нічого з цього                 — тільки через @-згадку в чаті
     Цей коментар видаліть. -->

## Контекст

<Чому правило існує — одне-два речення, спираючись на materials/architecture-brief.md.>

## Правило

- <Конкретна перевірювана вимога: «вихідний HTTP — лише через postJson() з app/src/core/http.ts»>
- <Заборона разом з альтернативою: «не використовуй X — використовуй Y»>

## Як перевірити

- <Команда або ознака: `cd app && npm run check:rules` → 0 у рядку http-via-core>
